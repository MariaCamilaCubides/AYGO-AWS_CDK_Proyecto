{
    "name": "node_postgres",
    "version": "1.0.0",
    "description": "node postgres api",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "macPredeploy": "zip -r Lambda-Deployment.zip * -x *.zip *.log",
        "windowsPredeploy": "Compress-Archive -LiteralPath node_modules, index.js -DestinationPath lambda.zip"
    },
    "keywords": [
        "postgres"
    ],
    "author": "María Camila Cubides",
    "license": "ISC",
    "dependencies": {
        "aws-sdk": "^2.1265.0",
        "pg": "^8.8.0"
    }
}
